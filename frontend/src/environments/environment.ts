// Development config, used by `ng serve` and `ng build` (default
// configuration). Frontend and backend still run as separate
// processes/ports in dev, so this needs the full backend URL.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5252/api',
};
